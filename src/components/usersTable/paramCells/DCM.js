

export default function DCM({ userindex, fieldindex }) {
    return (
        <div className="rTableCell noBorder paramCell70">
            <input type="checkbox" userindex={userindex} fieldindex={fieldindex} />
        </div>
    );
}